if WITH_FONT_SERVER
# XXX: Make sure FONTSERVER is set to a valid xfs server.
# subdir('fs')
endif
# TODO: init_fpe failed for font path catalogue:/etc/X11/fontpath.d: error 86: No such file or directory
# subdir('xfont')
